{
  "schema": 2,
  "artifact_rollback": true,
  "schema_compatible": false,
  "database_restore": "backup",
  "irreversible_migration": true,
  "minimum_supported_version": "0.2.2",
  "migration_floor": "20260828000600",
  "rationale": "Protocol v2, durable replay, recovery invalidation, mandatory Valkey epochs, bounded signed-token expiry, and migration 006 form one security boundary. Earlier releases cannot enforce tenant-bound OpenID4VP AAD, complete refresh and client policy, encrypted-only TOTP, or explicit runtime state and must never reopen this database. Recovery requires the controller-verified pre-update database backup; artifact-only rollback is forbidden."
}
