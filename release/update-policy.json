{
  "schema": 2,
  "artifact_rollback": true,
  "schema_compatible": false,
  "database_restore": "backup",
  "irreversible_migration": true,
  "minimum_supported_version": "0.2.7",
  "migration_floor": "20260901000100",
  "rationale": "Protocol v3 and migrations through 20260901000100 form one security boundary. Earlier releases cannot enforce dynamic tenant-directory control, tenant-local runtime modules, tenant-bound security state, or deployment-bound administrator receipts and must never reopen this database. Recovery requires the controller-verified pre-update database backup; artifact-only rollback is forbidden."
}
